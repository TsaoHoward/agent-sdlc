# Traceability Artifacts

Proposal-linked metadata artifacts should live here using:

`<task_request_id>.json`

This path stays tracked in the repository because Phase 1 proposal branches are expected to surface machine-readable traceability here.
