# .agent-sdlc

This directory holds project-local state and traceability surfaces for the Phase 1 closed loop.

Tracked repository surfaces:
- `traceability/` for proposal-linked metadata that may be committed in proposal branches

Runtime-only local surfaces created by bootstrap scripts:
- `state/task-requests/`
- `state/agent-sessions/`
- `dev-env/`
